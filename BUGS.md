# Known bugs

Last updated: 2026-09-24

| Severity | Issue | Notes |
| --- | --- | --- |
| --- | Resolved public-link drill rotation cursor collision | Confirmation previously used the pending public-link context hash, which is shared before the imported snapshot becomes confirmed. It now uses the importer-provided content hash for both confirmation and refresh persistence; direct form tests cover imported, malformed, and pasted cursor paths (2026-09-22). |
| ⭐⭐⭐ | Live Android call can capture audio but recognize only part of the trainee answer | The 2026-09-24 phone recording and matching completed AssemblyAI session `sess_762829d67bc348f7b5a4535d580a7229` rule out a connection or transcript-rendering failure: the provider applied 24 kHz PCM, `continuous_partials: true`, and VAD 0.3; its stored user channel contains audio, but its timeline produced only one user transcript ("What?") before the follow-up. The UI's "Voice signal detected" indicator currently means only non-zero PCM bytes left the browser, not that provider speech detection or transcription succeeded. **Diagnostic instrumentation now separates local audio sent, provider speech detection, and transcript receipt in the active call without saving raw audio. Open pending a physical-phone run that captures those three states; use that evidence before another VAD/worklet/transcription change.** |
| --- | Resolved clarity score rewarding empty answers | The deterministic evaluator now requires a supported FAQ fact and no unsupported claim before direct, concise sentence style can add clarity points (2026-09-22). |
| --- | Resolved JavaScript-rendered FAQ import producing garbage facts | equityedge.io/faq is a Framer hub: its visible labels link as `./general/...` to source-grounded article pages. The importer now reads those bounded, same-origin article links, extracts Framer's embedded rich-text state, and uses real article facts instead of the hub menu or footer. It also skips the optional LLM when deterministic article facts exist, avoiding a provider delay. Local live reproduction on 2026-09-21: 9 pages read, 14 article facts, no KYC→Slippage pair, no newsletter footer fact, 3.1 seconds. Gemini environment variables are configured in Vercel Production; model output still passes the deterministic grounding layer when it is needed. |
| --- | Resolved factual accuracy scoring 0 on imported pages | The HTML cleaner collapsed entire pages into a single line, so imports became one page-length "Reference detail" blob that no trainee answer could match. Block-level tags now preserve paragraph structure (2026-09-18); per-section facts make lexical matching possible. |
| --- | Resolved unreadable next exercise on long fact answers | The suggestion quoted the full missed-fact answer. It now quotes at most 180 characters (evaluator) and the report clamps display with a Show-full toggle (2026-09-18). |
| --- | Resolved home page "typed fallback" copy | The home screen advertised a typed-response option that live practice deliberately does not have. The clause was removed on 2026-09-17 with the product lead's approval; the copy now reads "practice your response by voice." |
| --- | Resolved Android Chrome speaker output | Provider PCM streaming, native 24 kHz Chromium playback, and live transcript deltas were verified on a physical phone by the product lead on 2026-09-17. |
| --- | Resolved customer audio fragmentation | PCM reply chunks now share one scheduled playback timeline, and microphone capture uses the browser default sample rate before resampling to 24 kHz for the provider. |
| --- | Resolved duplicate microphone start control | Voice practice begins with one `Join voice call` action; active calls show only Mute/Unmute and End call. |
| --- | Resolved unsupported live text turns | The live call UI is voice-only and no longer sends unsupported text-turn protocol events. |
| --- | Resolved live AssemblyAI browser startup failure | Firefox and Chromium rejected a detached browser `fetch` call before the token request left the page. The adapter now binds `fetch` to `globalThis`; regression coverage was added on 2026-09-17. |
| --- | No open Task 5 review issues | Console startup ownership was fixed on 2026-09-16. Earlier address validation, DNS rebinding, audio cancellation, live mute, adapter startup, typed fallback, and remote termination findings were fixed on 2026-09-15; regression evidence is in `.superpowers/sdd/task-5-report.md`. |
| --- | No open Task 6 evaluator issue | Relation-anchored matching now drives factual scoring. It passed the four diagnostic cases, 39 matcher tests, 24 evaluator tests, 12 evaluation-route tests, 190 full-suite tests, TypeScript checking, and a production build on 2026-09-17. The deterministic paraphrase limitation remains documented in the matcher report. |
